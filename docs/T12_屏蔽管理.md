# T12 — 负向反馈 & 关键词屏蔽

## 完成时间
2026-02-26

## 做了什么

### 1. 屏蔽过滤集成
在 `News.list_news_paginated/1` 中新增 `user_id` 参数，调用 `apply_blocking_filters/2`：

| 屏蔽类型 | 过滤方式 |
|----------|----------|
| keyword | 标题和中文标题 LIKE 匹配 |
| domain | 精确匹配域名 |
| tag | 子查询排除含该标签的新闻 |

### 2. BlockedLive.Index 管理页面
- 类型选择器：关键词/域名/标签，带图标
- 添加表单 + 动态占位符
- 已有规则列表，按类型着色（关键词黄、域名蓝、标签红）
- 一键删除

### 3. 路由和导航
- `/blocked` 路由在 `require_authenticated_user` 下
- 导航栏增加"屏蔽管理"入口

## 测试结果
159 tests, 0 failures（新增 9 个屏蔽相关测试）
